<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.legendtitans.bigfootbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigfootBook">
        <activity android:name=".Page5BigfootActivity"></activity>
        <activity android:name=".PageFourBigfootBook" />
        <activity android:name=".PageThreeBigfootBook" />
        <activity android:name=".PageTwoBigfootBook" />
        <activity android:name=".PageOneBigfootBook" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>